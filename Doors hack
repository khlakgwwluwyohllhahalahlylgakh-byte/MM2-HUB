-- ==========================================================
-- 🚪 DOORS MASTER AI BOT v12.0 PROFESSIONAL EDITION
-- ==========================================================
-- ✅ Sahte Kapı Algılama & Kaçış
-- ✅ Seek/Rush/Ambush Entity Yönetimi
-- ✅ Advanced Library Puzzle Solver
-- ✅ Anti-Crash Error Handling
-- ✅ Smooth Movement & Collision Fix
-- ✅ Full Map Navigation
-- ✅ Statistics & Logging
-- ==========================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

-- ==========================================================
-- GLOBAL STATE & SETTINGS
-- ==========================================================
_G.DoorsAI_v12 = {
    Running = true,
    Version = "12.0",
    LastError = nil,
    ErrorCount = 0
}

_G.Settings = {
    -- Bot Control
    AIEnabled = false,
    AutoPlayMode = true,
    
    -- Movement
    WalkSpeed = 25,
    SprintSpeed = 45,
    FlyMode = false,
    FlyHeight = 3.5,
    CollisionMode = "safe", -- "safe", "ghost", "normal"
    
    -- Detection & Avoidance
    EntityESP = false,
    FakeDoorDetection = true,
    SeekDetection = true,
    RushDetection = true,
    AmbushDetection = true,
    
    -- Visuals
    Fullbright = false,
    HighlightEntities = false,
    ShowPath = false,
    
    -- Features
    AutoSolveLibrary = true,
    AutoHideInClosets = true,
    AutoPickupKeys = true,
    
    -- Debug
    DebugMode = false,
    VerboseLogging = false
}

_G.Statistics = {
    StartTime = tick(),
    RoomsCompleted = 0,
    DoorsOpened = 0,
    KeysCollected = 0,
    EntitiesAvoided = 0,
    LibrariesSolved = 0,
    TimesHidden = 0,
    DistanceTraveled = 0,
    Crashes = 0,
    Errors = 0
}

_G.GameState = {
    CurrentRoom = nil,
    CurrentRoomNumber = 0,
    LastRoomNumber = -1,
    HasKey = false,
    IsHiding = false,
    ActiveEntity = nil,
    EntityType = nil,
    GameOver = false,
    Difficulty = 1
}

_G.FakeDoorDatabase = {
    -- Texture-based detection
    SuspiciousTextures = {"Concrete", "Brick", "Default"},
    -- Sound-based detection
    NoSoundDoors = {},
    -- Position-based detection
    UnrealisticPositions = {}
}

-- ==========================================================
-- RAYFIELD UI SETUP (GÜNCELLEŞTİRİLDİ)
-- ==========================================================
local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/UI-Libraries/Rayfield/main/source'))()

local Window = Rayfield:CreateWindow({
    Name = "🚪 DOORS MASTER AI v12.0",
    LoadingTitle = "Profesyonel AI Bot Yükleniyor...",
    LoadingSubtitle = "Advanced Detection & Solving Systems",
    ConfigurationSaving = { Enabled = true, FolderName = "DoorsAI", FileName = "config.json" },
    KeySystem = false
})

local MainTab = Window:CreateTab("🤖 Ana Kontrol")
local AITab = Window:CreateTab("🧠 Yapay Zeka")
local DetectionTab = Window:CreateTab("🎯 Algılama")
local LibraryTab = Window:CreateTab("📚 Kütüphane")
local VisualsTab = Window:CreateTab("👁️ Görseller")
local StatsTab = Window:CreateTab("📊 İstatistikler")

-- ==========================================================
-- LOGGING SYSTEM
-- ==========================================================
local function Log(message, level)
    level = level or "INFO"
    if _G.Settings.DebugMode or _G.Settings.VerboseLogging then
        local timestamp = os.date("%H:%M:%S")
        local prefix = string.format("[%s] [%s]", timestamp, level)
        print(prefix .. " " .. message)
    end
end

local function Error(message, critical)
    _G.Statistics.Errors = _G.Statistics.Errors + 1
    Log("[ERROR] " .. message, "ERROR")
    if critical then
        _G.DoorsAI_v12.LastError = message
        Rayfield:Notify({
            Title = "⚠️ Kritik Hata",
            Content = message,
            Duration = 5
        })
    end
end

local function Success(message)
    Log("[SUCCESS] " .. message, "SUCCESS")
end

-- ==========================================================
-- UTILITY FUNCTIONS
-- ==========================================================
local function SafeExecute(func, errorName)
    errorName = errorName or "Unknown Function"
    local success, result = pcall(func)
    if not success then
        Error("[" .. errorName .. "] " .. tostring(result), false)
        return nil
    end
    return result
end

local function GetHumanoidRootPart()
    if not Character or not Character.Parent then
        Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    end
    return Character:FindFirstChild("HumanoidRootPart")
end

local function GetHumanoid()
    if not Character or not Character.Parent then
        Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    end
    return Character:FindFirstChild("Humanoid")
end

local function GetCurrentRoom()
    local gameData = ReplicatedStorage:FindFirstChild("GameData")
    if gameData and gameData:FindFirstChild("LatestRoom") then
        local roomNum = gameData.LatestRoom.Value
        _G.GameState.CurrentRoomNumber = roomNum
        local room = Workspace.CurrentRooms:FindFirstChild(tostring(roomNum))
        return room, roomNum
    end
    return nil, 0
end

local function Distance(pos1, pos2)
    return (pos1 - pos2).Magnitude
end

local function Lerp(a, b, t)
    return a + (b - a) * math.min(math.max(t, 0), 1)
end

-- ==========================================================
-- SMOOTH MOVEMENT SYSTEM
-- ==========================================================
local function WalkToTarget(targetPos, timeout, stoppingDistance)
    timeout = timeout or 10
    stoppingDistance = stoppingDistance or 3
    
    local hrp = GetHumanoidRootPart()
    local humanoid = GetHumanoid()
    
    if not hrp or not humanoid then return false end
    
    humanoid.WalkSpeed = _G.Settings.WalkSpeed
    humanoid:MoveTo(targetPos)
    
    local startTime = tick()
    while Distance(hrp.Position, targetPos) > stoppingDistance do
        if not _G.Settings.AIEnabled or tick() - startTime > timeout or not hrp or not hrp.Parent then
            humanoid.WalkSpeed = 16
            return false
        end
        task.wait(0.05)
    end
    
    humanoid.WalkSpeed = 16
    return true
end

local function FlyToTarget(targetPos, timeout, altitude)
    altitude = altitude or _G.Settings.FlyHeight
    timeout = timeout or 10
    
    local hrp = GetHumanoidRootPart()
    if not hrp then return false end
    
    local targetElevated = Vector3.new(targetPos.X, targetPos.Y + altitude, targetPos.Z)
    local startTime = tick()
    
    while Distance(hrp.Position, targetElevated) > 3 do
        if not _G.Settings.AIEnabled or tick() - startTime > timeout or not hrp or not hrp.Parent then
            return false
        end
        
        local direction = (targetElevated - hrp.Position).Unit
        hrp.CFrame = CFrame.new(hrp.Position + direction * (_G.Settings.WalkSpeed * 0.022))
        task.wait(0.015)
    end
    
    return true
end

-- ==========================================================
-- FAKE DOOR DETECTION SYSTEM
-- ==========================================================
local function AnalyzeDoor(doorPart, doorModel)
    if not doorPart or not doorModel then return false end
    
    local analysis = {
        IsFake = false,
        Confidence = 0,
        Reasons = {}
    }
    
    -- [1] Texture Check
    local canvasSize = 0
    for _, part in pairs(doorModel:GetDescendants()) do
        if part:IsA("BasePart") then
            local texture = part.Material
            if texture == Enum.Material.Brick or texture == Enum.Material.Concrete then
                canvasSize = canvasSize + 1
            end
        end
    end
    
    if canvasSize > 2 then
        analysis.Confidence = analysis.Confidence + 0.3
        table.insert(analysis.Reasons, "Şüpheli Doku")
    end
    
    -- [2] Size Check
    local doorSize = doorPart.Size
    if doorSize.X < 4 or doorSize.Y < 6 then
        analysis.Confidence = analysis.Confidence + 0.4
        table.insert(analysis.Reasons, "Gerçekçi Olmayan Boyut")
    end
    
    -- [3] Position Check
    local doorPos = doorPart.Position
    local ray = Ray.new(doorPos, Vector3.new(0, -10, 0))
    local hit, hitPos = Workspace:FindPartOnRay(ray, Character)
    
    if hit and hit.Parent == doorModel.Parent then
        analysis.Confidence = analysis.Confidence + 0.25
        table.insert(analysis.Reasons, "Duvarda Batılı")
    end
    
    -- [4] Sound Check
    local hasSound = false
    for _, obj in pairs(doorModel:GetDescendants()) do
        if obj:IsA("Sound") then
            hasSound = true
            break
        end
    end
    
    if not hasSound then
        analysis.Confidence = analysis.Confidence + 0.2
        table.insert(analysis.Reasons, "Kapı Sesi Yok")
    end
    
    -- [5] Prompt Check
    local hasPrompt = false
    for _, obj in pairs(doorModel:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            hasPrompt = true
            if obj.HoldDuration > 0.5 then
                analysis.Confidence = analysis.Confidence + 0.15
                table.insert(analysis.Reasons, "Anormal Prompt Süresi")
            end
            break
        end
    end
    
    if not hasPrompt then
        analysis.Confidence = analysis.Confidence + 0.3
        table.insert(analysis.Reasons, "Prompt Bulunamadı")
    end
    
    analysis.IsFake = analysis.Confidence >= 0.6
    
    Log("Kapı Analizi [" .. doorModel.Name .. "] - Sahte: " .. tostring(analysis.IsFake) 
        .. " (" .. math.floor(analysis.Confidence * 100) .. "%) - Sebep: " .. table.concat(analysis.Reasons, ", "), "DETECTION")
    
    return analysis.IsFake, analysis.Confidence, analysis.Reasons
end

local function FindNextDoor(room, hrp)
    if not room or not hrp then return nil end
    
    local bestDoor = nil
    local shortestDistance = math.huge
    
    SafeExecute(function()
        local doors = room:FindFirstChild("Doors")
        if doors then
            for _, doorModel in pairs(doors:GetChildren()) do
                if doorModel.Name == "Door" then
                    local doorPart = doorModel:FindFirstChildWhichIsA("BasePart")
                    if doorPart then
                        local dist = Distance(hrp.Position, doorPart.Position)
                        
                        if _G.Settings.FakeDoorDetection then
                            local isFake = AnalyzeDoor(doorPart, doorModel)
                            if isFake then
                                Log("🚫 SAHTE KAPI SEÇİLMİYOR!", "WARNING")
                                goto skip_door
                            end
                        end
                        
                        if dist < shortestDistance then
                            shortestDistance = dist
                            bestDoor = doorModel
                        end
                        
                        ::skip_door::
                    end
                end
            end
        end
    end, "FindNextDoor")
    
    return bestDoor
end

-- ==========================================================
-- ENTITY DETECTION & MANAGEMENT
-- ==========================================================
local function DetectSeek()
    local seek = Workspace:FindFirstChild("SeekMoving") or Workspace:FindFirstChild("SeekRig")
    if seek then
        _G.GameState.ActiveEntity = seek
        _G.GameState.EntityType = "Seek"
        return true, seek
    end
    return false, nil
end

local function DetectRush()
    local rush = Workspace:FindFirstChild("RushMoving")
    if rush then
        _G.GameState.ActiveEntity = rush
        _G.GameState.EntityType = "Rush"
        return true, rush
    end
    return false, nil
end

local function DetectAmbush()
    local ambush = Workspace:FindFirstChild("AmbushMoving")
    if ambush then
        _G.GameState.ActiveEntity = ambush
        _G.GameState.EntityType = "Ambush"
        return true, ambush
    end
    return false, nil
end

local function GetEntityPosition(entity)
    if not entity then return nil end
    
    local pos = nil
    SafeExecute(function()
        if entity:FindFirstChild("HumanoidRootPart") then
            pos = entity.HumanoidRootPart.Position
        elseif entity:FindFirstChild("Torso") then
            pos = entity.Torso.Position
        elseif entity:FindFirstChild("Head") then
            pos = entity.Head.Position
        else
            for _, part in pairs(entity:GetChildren()) do
                if part:IsA("BasePart") then
                    pos = part.Position
                    break
                end
            end
        end
    end, "GetEntityPosition")
    
    return pos
end

-- ==========================================================
-- HIDING SYSTEM (Closet/Wardrobe)
-- ==========================================================
local function FindHidingSpot(room, hrp)
    if not room or not hrp then return nil, nil end
    
    local bestHiding = nil
    local shortestDist = math.huge
    
    SafeExecute(function()
        for _, obj in pairs(room:GetDescendants()) do
            if obj.Name:find("Wardrobe") or obj.Name:find("Locker") or obj.Name:find("Cabinet") or obj.Name:find("Closet") then
                local part = obj:FindFirstChildWhichIsA("BasePart")
                if part then
                    local dist = Distance(hrp.Position, part.Position)
                    if dist < shortestDist and dist < 50 then
                        shortestDist = dist
                        bestHiding = obj
                    end
                end
            end
        end
    end, "FindHidingSpot")
    
    if bestHiding then
        local hidingPart = bestHiding:FindFirstChildWhichIsA("BasePart", true)
        local prompt = bestHiding:FindFirstChildWhichIsA("ProximityPrompt", true)
        return bestHiding, { part = hidingPart, prompt = prompt }
    end
    
    return nil, nil
end

local function HideInCloset(room, hrp)
    if not room or not hrp or not _G.Settings.AutoHideInClosets then return false end
    
    Log("🚪 Gizli yer aranıyor...", "ACTION")
    
    local hidingObj, hidingData = FindHidingSpot(room, hrp)
    if not hidingObj or not hidingData.part or not hidingData.prompt then
        Log("❌ Gizli yer bulunamadı!", "WARNING")
        return false
    end
    
    WalkToTarget(hidingData.part.Position, 15, 2)
    task.wait(0.2)
    
    SafeExecute(function()
        if hidingData.prompt.TriggerPrompt then
            hidingData.prompt:TriggerPrompt()
        else
            fireproximityprompt(hidingData.prompt)
        end
    end, "HidePrompt")
    
    _G.GameState.IsHiding = true
    _G.Statistics.TimesHidden = _G.Statistics.TimesHidden + 1
    Success("🚪 Gizli yere kaçıldı!")
    
    return true
end

-- ==========================================================
-- LIBRARY PUZZLE SOLVER
-- ==========================================================
local _LibraryData = {
    ShapeOrder = {},
    ShapeNumbers = {},
    Password = ""
}

local function ScanLibraryShapeOrder()
    _LibraryData.ShapeOrder = {}
    
    SafeExecute(function()
        local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
        if playerGui then
            for _, screen in pairs(playerGui:GetDescendants()) do
                if screen:IsA("Frame") or screen:IsA("ImageLabel") then
                    if screen.Name:find("Heart") or screen.Name:find("Hint") then
                        local shapeName = screen.Name
                        if shapeName:find("Circle") then table.insert(_LibraryData.ShapeOrder, "Circle")
                        elseif shapeName:find("Square") then table.insert(_LibraryData.ShapeOrder, "Square")
                        elseif shapeName:find("Triangle") then table.insert(_LibraryData.ShapeOrder, "Triangle")
                        elseif shapeName:find("Star") then table.insert(_LibraryData.ShapeOrder, "Star")
                        elseif shapeName:find("Diamond") then table.insert(_LibraryData.ShapeOrder, "Diamond")
                        elseif shapeName:find("Cross") then table.insert(_LibraryData.ShapeOrder, "Cross")
                        elseif shapeName:find("Pentagon") then table.insert(_LibraryData.ShapeOrder, "Pentagon")
                        elseif shapeName:find("Hexagon") then table.insert(_LibraryData.ShapeOrder, "Hexagon")
                        end
                    end
                end
            end
        end
    end, "ScanLibraryShapeOrder")
    
    Log("📚 Shape Order Tarandı: " .. table.concat(_LibraryData.ShapeOrder, ", "), "LIBRARY")
end

local function ReadLibraryBooks(room)
    if not room then return end
    
    _LibraryData.ShapeNumbers = {}
    
    SafeExecute(function()
        local books = {}
        for _, obj in pairs(room:GetDescendants()) do
            if obj.Name:find("Book") or obj.Name:find("Hint") then
                table.insert(books, obj)
            end
        end
        
        for _, book in pairs(books) do
            local prompt = book:FindFirstChildWhichIsA("ProximityPrompt", true)
            if prompt then
                local bookPart = book:FindFirstChildWhichIsA("BasePart", true) or book
                
                WalkToTarget(bookPart.Position, 5, 2)
                task.wait(0.1)
                
                if prompt.TriggerPrompt then
                    prompt:TriggerPrompt()
                else
                    fireproximityprompt(prompt)
                end
                
                task.wait(0.3)
                
                local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
                if playerGui then
                    for _, textObj in pairs(playerGui:GetDescendants()) do
                        if textObj:IsA("TextLabel") then
                            local text = textObj.Text:lower()
                            local number = text:match("%d+")
                            
                            if number and (text:find("circle") or text:find("square") or text:find("triangle") 
                                or text:find("star") or text:find("diamond") or text:find("cross")
                                or text:find("pentagon") or text:find("hexagon")) then
                                
                                if text:find("circle") then _LibraryData.ShapeNumbers["Circle"] = tonumber(number)
                                elseif text:find("square") then _LibraryData.ShapeNumbers["Square"] = tonumber(number)
                                elseif text:find("triangle") then _LibraryData.ShapeNumbers["Triangle"] = tonumber(number)
                                elseif text:find("star") then _LibraryData.ShapeNumbers["Star"] = tonumber(number)
                                elseif text:find("diamond") then _LibraryData.ShapeNumbers["Diamond"] = tonumber(number)
                                elseif text:find("cross") then _LibraryData.ShapeNumbers["Cross"] = tonumber(number)
                                elseif text:find("pentagon") then _LibraryData.ShapeNumbers["Pentagon"] = tonumber(number)
                                elseif text:find("hexagon") then _LibraryData.ShapeNumbers["Hexagon"] = tonumber(number)
                                end
                            end
                        end
                    end
                end
                
                task.wait(0.2)
            end
        end
    end, "ReadLibraryBooks")
    
    Log("📚 Kitaplar Okundu", "LIBRARY")
end

local function GenerateLibraryPassword()
    _LibraryData.Password = ""
    
    for _, shape in pairs(_LibraryData.ShapeOrder) do
        if _LibraryData.ShapeNumbers[shape] then
            _LibraryData.Password = _LibraryData.Password .. tostring(_LibraryData.ShapeNumbers[shape])
        end
    end
    
    Log("📚 Şifre Üretildi: " .. _LibraryData.Password, "LIBRARY")
    return _LibraryData.Password
end

local function SolveLibrary(room)
    if not room or room.Name ~= "50" then return false end
    
    Log("🔐 Kütüphane Çözme Başladı...", "LIBRARY")
    
    local hrp = GetHumanoidRootPart()
    local humanoid = GetHumanoid()
    if not hrp or not humanoid then return false end
    
    if _G.Settings.AutoSolveLibrary then
        SafeExecute(function()
            ReplicatedStorage.RemotesFolder.Crouch:FireServer(true)
        end, "LibraryCrouch")
        
        task.wait(0.3)
        
        SafeExecute(function()
            for _, obj in pairs(room:GetDescendants()) do
                if obj.Name:find("Paper") or obj.Name:find("Hint") then
                    local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if prompt then
                        WalkToTarget(obj.Position, 5, 2)
                        task.wait(0.1)
                        if prompt.TriggerPrompt then
                            prompt:TriggerPrompt()
                        else
                            fireproximityprompt(prompt)
                        end
                        task.wait(0.2)
                    end
                end
            end
        end, "ReadPaper")
        
        task.wait(0.2)
        ScanLibraryShapeOrder()
        task.wait(0.3)
        ReadLibraryBooks(room)
        task.wait(0.3)
        
        local password = GenerateLibraryPassword()
        
        if password and password ~= "" then
            SafeExecute(function()
                local padlock = room:FindFirstChild("Padlock") or room:FindFirstChild("Lock")
                if padlock then
                    local lockPart = padlock:FindFirstChildWhichIsA("BasePart", true)
                    if lockPart then
                        WalkToTarget(lockPart.Position, 5, 2)
                        task.wait(0.2)
                        
                        ReplicatedStorage.RemotesFolder.PL:FireServer(password)
                        Success("✅ Şifre Gönderildi: " .. password)
                        _G.Statistics.LibrariesSolved = _G.Statistics.LibrariesSolved + 1
                    end
                end
            end, "EnterPassword")
        else
            Error("Şifre oluşturulamadı!", false)
        end
        
        task.wait(0.3)
        
        SafeExecute(function()
            ReplicatedStorage.RemotesFolder.Crouch:FireServer(false)
        end, "LibraryCrouchEnd")
    end
    
    return true
end

-- ==========================================================
-- KEY MANAGEMENT
-- ==========================================================
local function FindKeyInRoom(room)
    if not room then return nil, nil end
    
    local bestKey = nil
    local shortestDist = math.huge
    
    SafeExecute(function()
        for _, obj in pairs(room:GetDescendants()) do
            if obj.Name:find("Key") then
                local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true)
                if prompt then
                    local keyPart = obj:FindFirstChildWhichIsA("BasePart", true) or obj
                    local dist = Distance(GetHumanoidRootPart().Position, keyPart.Position)
                    if dist < shortestDist then
                        shortestDist = dist
                        bestKey = { obj = obj, prompt = prompt, part = keyPart }
                    end
                end
            end
        end
    end, "FindKeyInRoom")
    
    return bestKey
end

local function PickupKey(room)
    if not _G.Settings.AutoPickupKeys then return end
    
    local keyData = FindKeyInRoom(room)
    if not keyData then return end
    
    WalkToTarget(keyData.part.Position, 5, 2)
    task.wait(0.1)
    
    SafeExecute(function()
        if keyData.prompt.TriggerPrompt then
            keyData.prompt:TriggerPrompt()
        else
            fireproximityprompt(keyData.prompt)
        end
    end, "PickupKey")
    
    _G.Statistics.KeysCollected = _G.Statistics.KeysCollected + 1
    task.wait(0.3)
end

local function HasKey()
    if not Character then return false end
    return Character:FindFirstChild("Key") ~= nil or LocalPlayer.Backpack:FindFirstChild("Key") ~= nil
end

-- ==========================================================
-- MAIN GAME LOOP
-- ==========================================================
task.spawn(function()
    while _G.DoorsAI_v12.Running do
        task.wait(0.05)
        
        if not _G.Settings.AIEnabled then
            task.wait(0.5)
            goto continue
        end
        
        SafeExecute(function()
            local room, roomNum = GetCurrentRoom()
            if not room then goto continue end
            
            local hrp = GetHumanoidRootPart()
            local humanoid = GetHumanoid()
            if not hrp or not humanoid then goto continue end
            
            if roomNum ~= _G.GameState.LastRoomNumber then
                _G.GameState.LastRoomNumber = roomNum
                _G.Statistics.RoomsCompleted = roomNum
                Log("📍 Oda Girişi: " .. roomNum, "ROOM")
                _G.GameState.IsHiding = false
            end
            
            -- [DURUM 1]: SEEK KAÇIŞI
            if _G.Settings.SeekDetection and DetectSeek() then
                Log("🏃 SEEK SALDIRISI! KAÇILIYOR...", "THREAT")
                humanoid.WalkSpeed = math.max(_G.Settings.SprintSpeed, 45)
                local entity = _G.GameState.ActiveEntity
                if entity then
                    local entityPos = GetEntityPosition(entity)
                    if entityPos then
                        local escapeDir = (hrp.Position - entityPos).Unit
                        local escapePos = hrp.Position + escapeDir * 100
                        WalkToTarget(escapePos, 30, 5)
                    end
                end
                _G.Statistics.EntitiesAvoided = _G.Statistics.EntitiesAvoided + 1
                goto continue
            end
            
            -- [DURUM 2]: RUSH KAÇIŞI
            if _G.Settings.RushDetection and DetectRush() then
                Log("⚡ RUSH UYARISI! TEZDEN GİT!", "THREAT")
                humanoid.WalkSpeed = _G.Settings.SprintSpeed
                local door = room:FindFirstChild("Door")
                if door then
                    local doorPart = door:FindFirstChildWhichIsA("BasePart")
                    if doorPart then
                        WalkToTarget(doorPart.Position, 15, 3)
                    end
                end
                _G.Statistics.EntitiesAvoided = _G.Statistics.EntitiesAvoided + 1
                goto continue
            end
            
            -- [DURUM 3]: AMBUSH KAÇIŞI
            if _G.Settings.AmbushDetection and DetectAmbush() then
                Log("🎭 AMBUSH SALDIRISI! SAKLANILIYOR...", "THREAT")
                HideInCloset(room, hrp)
                _G.Statistics.EntitiesAvoided = _G.Statistics.EntitiesAvoided + 1
                goto continue
            end
            
            -- [DURUM 4]: KÜTÜPHANE ODA (50)
            if room.Name == "50" then
                SolveLibrary(room)
                goto continue
            end
            
            -- [DURUM 5]: NORMAL ODA İŞLEMİ
            humanoid.WalkSpeed = _G.Settings.WalkSpeed
            PickupKey(room)
            
            local nextDoor = FindNextDoor(room, hrp)
            if nextDoor then
                local doorPart = nextDoor:FindFirstChildWhichIsA("BasePart")
                if doorPart then
                    WalkToTarget(doorPart.Position, 10, 2)
                    task.wait(0.1)
                    
                    local lock = nextDoor:FindFirstChild("Lock")
                    if lock and not HasKey() then
                        Log("🔓 Anahtar gerekli, bulunmuyor!", "WARNING")
                    elseif lock and HasKey() then
                        local lockPrompt = lock:FindFirstChildWhichIsA("ProximityPrompt", true)
                        if lockPrompt then
                            local keyTool = LocalPlayer.Backpack:FindFirstChild("Key")
                            if keyTool then
                                humanoid:EquipTool(keyTool)
                                task.wait(0.1)
                            end
                            
                            SafeExecute(function()
                                if lockPrompt.TriggerPrompt then
                                    lockPrompt:TriggerPrompt()
                                else
                                    fireproximityprompt(lockPrompt)
                                end
                            end, "UnlockDoor")
                            task.wait(0.2)
                        end
                    end
                    
                    local doorPrompt = doorPart.Parent:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if doorPrompt then
                        SafeExecute(function()
                            if doorPrompt.TriggerPrompt then
                                doorPrompt:TriggerPrompt()
                            else
                                fireproximityprompt(doorPrompt)
                            end
                        end, "OpenDoor")
                    end
                    
                    _G.Statistics.DoorsOpened = _G.Statistics.DoorsOpened + 1
                    task.wait(0.5)
                end
            end
            
        end, "MainGameLoop")
        
        ::continue::
    end
end)

-- ==========================================================
-- VISUAL & LIGHTING SYSTEM
-- ==========================================================
task.spawn(function()
    while _G.DoorsAI_v12.Running do
        task.wait(1)
        
        SafeExecute(function()
            if _G.Settings.Fullbright then
                Lighting.Brightness = 2.5
                Lighting.ClockTime = 14
                Lighting.GlobalShadows = false
                Lighting.Ambient = Color3.fromRGB(255, 255, 255)
                Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
            else
                Lighting.Brightness = 1
                Lighting.ClockTime = 12
                Lighting.GlobalShadows = true
            end
        end, "LightingSystem")
        
        if _G.Settings.HighlightEntities then
            SafeExecute(function()
                for _, entity in pairs(Workspace:GetChildren()) do
                    if entity.Name == "SeekMoving" or entity.Name == "SeekRig" or 
                       entity.Name == "RushMoving" or entity.Name == "AmbushMoving" then
                        
                        if not entity:FindFirstChild("EntityHighlight") then
                            local highlight = Instance.new("Highlight")
                            highlight.Name = "EntityHighlight"
                            highlight.FillColor = Color3.fromRGB(255, 0, 0)
                            highlight.OutlineColor = Color3.fromRGB(255, 100, 0)
                            highlight.FillTransparency = 0.4
                            highlight.OutlineTransparency = 0.1
                            highlight.Parent = entity
                        end
                    end
                end
            end, "EntityHighlight")
        end
    end
end)

-- ==========================================================
-- UI CONTROLS & BINDINGS
-- ==========================================================
MainTab:CreateSection("🎮 Bot Durumu")
local BotStatusLabel = MainTab:CreateLabel("⏹️ Kapalı")

MainTab:CreateToggle({
    Name = "🤖 Yapay Zeka Aç/Kapat",
    CurrentValue = false,
    Flag = "AIEnabled",
    Callback = function(Value)
        _G.Settings.AIEnabled = Value
        pcall(function() BotStatusLabel:Set(Value and "✅ Çalışıyor" or "⏹️ Kapalı") end)
        Rayfield:Notify({
            Title = Value and "✅ AI Açıldı" or "⏹️ AI Kapandı",
            Content = Value and "Bot oyunu oynayacak" or "Bot durduruldu",
            Duration = 2
        })
    end
})

MainTab:CreateSection("⚙️ Hızlı Ayarlar")
MainTab:CreateSlider({
    Name = "🚶 Yürüyüş Hızı",
    Range = {16, 50},
    Increment = 1,
    CurrentValue = 25,
    Flag = "WalkSpeed",
    Callback = function(Value)
        _G.Settings.WalkSpeed = Value
    end
})

MainTab:CreateSlider({
    Name = "🏃 Sprint Hızı",
    Range = {30, 100},
    Increment = 1,
    CurrentValue = 45,
    Flag = "SprintSpeed",
    Callback = function(Value)
        _G.Settings.SprintSpeed = Value
    end
})

AITab:CreateSection("🧠 AI İşlevleri")
AITab:CreateToggle({
    Name = "🔐 Otomatik Kütüphane Çözme",
    CurrentValue = true,
    Flag = "AutoSolveLibrary",
    Callback = function(Value)
        _G.Settings.AutoSolveLibrary = Value
    end
})

AITab:CreateToggle({
    Name = "🔑 Otomatik Anahtar Toplama",
    CurrentValue = true,
    Flag = "AutoPickupKeys",
    Callback = function(Value)
        _G.Settings.AutoPickupKeys = Value
    end
})

AITab:CreateToggle({
    Name = "🚪 Otomatik Saklanma",
    CurrentValue = true,
    Flag = "AutoHideInClosets",
    Callback = function(Value)
        _G.Settings.AutoHideInClosets = Value
    end
})

DetectionTab:CreateSection("🎯 Tehdit Algılaması")
DetectionTab:CreateToggle({
    Name = "👹 Seek Algılaması",
    CurrentValue = true,
    Flag = "SeekDetection",
    Callback = function(Value)
        _G.Settings.SeekDetection = Value
    end
})

DetectionTab:CreateToggle({
    Name = "⚡ Rush Algılaması",
    CurrentValue = true,
    Flag = "RushDetection",
    Callback = function(Value)
        _G.Settings.RushDetection = Value
    end
})

DetectionTab:CreateToggle({
    Name = "🎭 Ambush Algılaması",
    CurrentValue = true,
    Flag = "AmbushDetection",
    Callback = function(Value)
        _G.Settings.AmbushDetection = Value
    end
})

DetectionTab:CreateToggle({
    Name = "🚪 Sahte Kapı Algılaması ⭐ ÖNEMLI",
    CurrentValue = true,
    Flag = "FakeDoorDetection",
    Callback = function(Value)
        _G.Settings.FakeDoorDetection = Value
    end
})

LibraryTab:CreateSection("📚 Kütüphane Çözücü")
local LibraryStatusLabel = LibraryTab:CreateLabel("⏳ Hazır...")

LibraryTab:CreateButton({
    Name = "🔍 Kütüphaneyi Manuel Çöz",
    Callback = function()
        local room = GetCurrentRoom()
        if room and room.Name == "50" then
            SolveLibrary(room)
            pcall(function() LibraryStatusLabel:Set("✅ Çözüldü!") end)
        else
            pcall(function() LibraryStatusLabel:Set("❌ Kütüphane Odası Değil") end)
        end
    end
})

VisualsTab:CreateSection("👁️ Görsel Ayarlar")
VisualsTab:CreateToggle({
    Name = "💡 Fullbright",
    CurrentValue = false,
    Flag = "Fullbright",
    Callback = function(Value)
        _G.Settings.Fullbright = Value
    end
})

VisualsTab:CreateToggle({
    Name = "👹 Entity Vurgula",
    CurrentValue = false,
    Flag = "HighlightEntities",
    Callback = function(Value)
        _G.Settings.HighlightEntities = Value
    end
})

StatsTab:CreateSection("📊 İstatistikler")
local StatsLabel = StatsTab:CreateLabel("Yükleniyor...")

task.spawn(function()
    while _G.DoorsAI_v12.Running do
        task.wait(2)
        local uptime = tick() - _G.Statistics.StartTime
        local hours = math.floor(uptime / 3600)
        local minutes = math.floor((uptime % 3600) / 60)
        local seconds = math.floor(uptime % 60)
        
        pcall(function()
            StatsLabel:Set(
                "📍 Odalar: " .. _G.Statistics.RoomsCompleted .. "\n" ..
                "🚪 Kapılar: " .. _G.Statistics.DoorsOpened .. "\n" ..
                "🔑 Anahtarlar: " .. _G.Statistics.KeysCollected .. "\n" ..
                "👹 Kaçışlar: " .. _G.Statistics.EntitiesAvoided .. "\n" ..
                "📚 Kütüphaneler: " .. _G.Statistics.LibrariesSolved .. "\n" ..
                "🚪 Saklanmalar: " .. _G.Statistics.TimesHidden .. "\n" ..
                "❌ Hatalar: " .. _G.Statistics.Errors .. "\n" ..
                "⏱️ Süre: " .. string.format("%02d:%02d:%02d", hours, minutes, seconds)
            )
        end)
    end
end)

Rayfield:Notify({
    Title = "✅ DOORS MASTER AI v12.0",
    Content = "Tüm sistemler yüklendi ve hazır!",
    Duration = 5
})

Log("✅ Bot tamamen yüklendi ve hazır!", "SUCCESS")

return _G.DoorsAI_v12
